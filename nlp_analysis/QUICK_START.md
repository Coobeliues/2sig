# 🚀 Быстрый старт

## Минимальные шаги для запуска MVP

### Шаг 1: Установка зависимостей

```bash
# Создание виртуального окружения (рекомендуется)
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Установка библиотек
pip install -r requirements.txt

# Установка spaCy модели для русского языка
python -m spacy download ru_core_news_sm
```

### Шаг 2: Проверка данных

Убедитесь, что у вас есть файлы:
- `reviews_full.csv` - отзывы
- `places.csv` - информация о заведениях

Необходимые колонки в `reviews_full.csv`:
- `place_id` - ID заведения
- `review_text` - текст отзыва
- `rating` - оценка (1-5)
- `date` - дата (опционально)

Необходимые колонки в `places.csv`:
- `id` - ID заведения
- `name` - название
- `address` - адрес
- `category` - категория
- `rating` - рейтинг

### Шаг 3: Запуск базового поиска

```bash
python semantic_search.py
```

При первом запуске:
1. Загрузится модель LaBSE (~500MB)
2. Создадутся эмбеддинги всех отзывов (может занять 5-15 минут)
3. Эмбеддинги сохранятся в `cache/embeddings.pkl` для быстрой загрузки в будущем

### Шаг 4: Запуск веб-интерфейса

```bash
streamlit run app.py
```

Откроется браузер с интерфейсом поиска.

## 🧪 Тестовые запросы

Попробуйте следующие запросы:

1. **Базовые:**
   - "уютное кафе с вкусным кофе"
   - "недорогой ресторан с хорошей едой"
   - "тихое место для работы"

2. **Специфичные:**
   - "заведение с живой музыкой"
   - "семейный ресторан с детской площадкой"
   - "романтическое место для свидания"

3. **По блюдам:**
   - "где можно поесть хороший стейк"
   - "кафе с вкусными десертами"
   - "ресторан с суши"

## 📊 Продвинутые фичи

### Детекция спама

```bash
python advanced_analytics.py
```

Запустит анализ:
- Подозрительных отзывов
- Заведений с накруткой
- Извлечение аспектов
- Популярные блюда

### Трекинг специалистов

```bash
python barber_tracker.py
```

Найдет упоминания специалистов (барберов, официантов) и отследит их перемещения.

## 🔧 Настройка

### Смена модели эмбеддингов

В `semantic_search.py` измените параметр `model_name`:

```python
# Быстрая модель (только русский)
search_engine = SemanticSearch(
    model_name='DeepPavlov/rubert-base-cased-sentence'
)

# Мультиязычная (рекомендуется)
search_engine = SemanticSearch(
    model_name='sentence-transformers/LaBSE'
)

# Высокое качество (медленнее)
search_engine = SemanticSearch(
    model_name='ai-forever/sbert_large_nlu_ru'
)
```

### Использование GPU

Если у вас есть NVIDIA GPU:

```bash
# Установите PyTorch с CUDA
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118

# Установите FAISS для GPU
pip uninstall faiss-cpu
pip install faiss-gpu
```

В `semantic_search.py`:
```python
search_engine.build_index(use_gpu=True)
```

## 🐛 Решение проблем

### Проблема: "ModuleNotFoundError: No module named 'sentence_transformers'"

**Решение:**
```bash
pip install sentence-transformers
```

### Проблема: "OSError: Can't find model 'ru_core_news_sm'"

**Решение:**
```bash
python -m spacy download ru_core_news_sm
```

### Проблема: "CUDA out of memory"

**Решение:**
- Уменьшите `batch_size` в `create_embeddings()`
- Или используйте CPU версию

### Проблема: Медленное создание эмбеддингов

**Решение:**
- Используйте GPU
- Уменьшите размер батча
- Или используйте более легкую модель

## 📝 Следующие шаги

После успешного запуска MVP:

1. **Улучшение качества:**
   - Экспериментируйте с разными моделями
   - Настройте параметры ранжирования
   - Добавьте фильтры

2. **Новые фичи:**
   - Реализуйте ABSA (анализ аспектов)
   - Добавьте детекцию спама
   - Создайте API

3. **Масштабирование:**
   - Оптимизируйте индекс FAISS
   - Добавьте кэширование
   - Разверните на сервере

См. [PROJECT_ROADMAP.md](PROJECT_ROADMAP.md) для детального плана.

## 💬 Нужна помощь?

- Проверьте [PROJECT_ROADMAP.md](PROJECT_ROADMAP.md) для детальной документации
- Посмотрите примеры кода в файлах
- Создайте issue на GitHub
