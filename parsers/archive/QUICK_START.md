# 🚀 Быстрый старт - Парсеры 2GIS

## 📌 Какой парсер использовать?

### 1️⃣ Одно заведение - ВСЕ отзывы
**Файл:** `parser_all_final.py`

```bash
python parser_all_final.py
```

**Результат:** ~2,300 отзывов с одного заведения
**Время:** ~30-40 секунд

---

### 2️⃣ Массовый сбор - МНОГО заведений
**Файл:** `parser_mass_collection.py`

```bash
python parser_mass_collection.py
```

**Результат:** ~84,000 отзывов с 97 заведений (10 категорий)
**Время:** ~20-30 минут

---

### 3️⃣ Простой парсер - Быстрая проба
**Файл:** `parser_final.py`

```bash
python parser_final.py
```

**Результат:** ~34-50 отзывов с одного заведения
**Время:** ~10 секунд

---

## 🎯 Для вашего NLP проекта

### Рекомендация:
Используйте **`parser_mass_collection.py`** - он собирает максимум данных со множества заведений.

### Что вы получите:
- ✅ **84,721 отзыв** с метаданными
- ✅ **97 заведений** из 10 категорий (кафе, рестораны, бары и т.д.)
- ✅ Все поля: автор, рейтинг, текст, дата, категория
- ✅ Готовый датасет для NLP анализа

---

## ⚙️ Настройка массового сбора

Откройте `parser_mass_collection.py` и измените:

```python
CITY = "almaty"  # Ваш город

CATEGORIES = [
    "кафе",
    "ресторан",
    "бар",
    # добавьте свои...
]

MAX_PER_CATEGORY = 10  # Количество заведений на категорию
```

---

## 📁 Файлы результатов

### Последний массовый сбор:
`2gis_mass_reviews_almaty_20251006_163719.json`

### Структура:
```json
[
  {
    "name": "Название заведения",
    "category": "Категория 2GIS",
    "category_search": "Категория поиска",
    "rating": 4.6,
    "reviews": [
      {
        "author": "Имя автора",
        "rating": 5,
        "text": "Текст отзыва...",
        "date": "2025-10-06",
        "is_verified": true
      }
    ]
  }
]
```

---

## 🔥 Быстрый анализ данных

```python
import json

# Загрузка
with open('2gis_mass_reviews_almaty_20251006_163719.json', 'r', encoding='utf-8') as f:
    data = json.load(f)

# Статистика
total_reviews = sum(len(p['reviews']) for p in data)
print(f"Всего отзывов: {total_reviews}")
print(f"Заведений: {len(data)}")

# Все тексты отзывов
texts = [r['text'] for p in data for r in p['reviews']]

# Только положительные (4-5 звезд)
positive = [r for p in data for r in p['reviews'] if r['rating'] >= 4]

# По категориям
cafes = [p for p in data if p['category_search'] == 'кафе']
```

---

## 💡 Советы

1. **Для быстрого теста:** используйте `parser_final.py`
2. **Для NLP проекта:** используйте `parser_mass_collection.py`
3. **Для конкретного заведения:** используйте `parser_all_final.py`
4. **Увеличьте `MAX_PER_CATEGORY`** для большего датасета (но увеличится время)
5. **Добавьте категории** в список `CATEGORIES` по вашему выбору

---

## ❓ Проблемы?

### API ключ перестал работать:
1. Откройте 2GIS в браузере
2. Откройте DevTools (F12) → Network
3. Найдите запросы к `reviews.2gis.com`
4. Скопируйте параметр `key=` из URL
5. Замените в коде: `'key': 'НОВЫЙ_КЛЮЧ'`

### Chrome не запускается:
Установите ChromeDriver:
```bash
pip install webdriver-manager
```

---

**Готово! Можете начинать сбор данных для вашего NLP проекта! 🎓**
