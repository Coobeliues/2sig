# MVP готов к использованию! 🎉

## Что создано

В директории `/home/keosido/Desktop/nlp_2gis/mvp/` находится полностью готовый пайплайн для семантического поиска заведений.

### Файлы

```
mvp/
├── config.py                 # Конфигурация (пути, модель, параметры)
├── step1_prepare.py          # Подготовка данных (запустить один раз)
├── step2_search.py           # Поиск (терминал)
├── search_demo.ipynb         # Демо ноутбук (Jupyter)
├── app.py                    # Веб-интерфейс (опционально)
├── requirements.txt          # Зависимости
├── README.md                 # Полная документация
├── QUICK_START.md            # Быстрый старт
└── MVP_COMPLETE.md           # Этот файл
```

### Особенности

✅ **Мультиязычность**: Поддержка русского и казахского языков
✅ **Эмодзи**: Сохранение и обработка эмодзи из отзывов
✅ **Быстрый поиск**: 50-200 мс на запрос через FAISS
✅ **Кэширование**: Embeddings создаются один раз, затем используется кэш
✅ **Гибкость**: 3 метода агрегации (weighted, mean, max)
✅ **Простота**: Работает на обычном ноутбуке (8GB RAM)

## Как запустить

### Вариант 1: Терминал (рекомендуется)

```bash
# 1. Установка
cd /home/keosido/Desktop/nlp_2gis/mvp
pip install -r requirements.txt

# 2. Подготовка данных (один раз, 30-60 минут)
python step1_prepare.py

# 3. Поиск
python step2_search.py
# Выберите режим 1 или 2
```

### Вариант 2: Jupyter Notebook

```bash
# Запустите Jupyter
jupyter notebook

# Откройте search_demo.ipynb
# Запускайте ячейки последовательно
```

### Вариант 3: В вашем коде

```python
from step2_search import SemanticSearch

# Инициализация
search = SemanticSearch()

# Поиск
results = search.search_places("уютное кафе с вкусным кофе", top_k=10)

# Результаты
for idx, row in results.iterrows():
    print(f"{idx+1}. {row['name']} - {row['final_score']:.3f}")
```

### Вариант 4: Веб-интерфейс (опционально)

```bash
# Установите дополнительные зависимости
pip install streamlit plotly

# Запустите
streamlit run app.py
```

## Что происходит под капотом

### Шаг 1: Подготовка (step1_prepare.py)

```
Отзывы (CSV)
    ↓
[SentenceTransformer LaBSE]
    ↓
Embeddings (768-dim векторы)
    ↓
[FAISS IndexFlatIP]
    ↓
cache/
├── embeddings.pkl      (~200 MB)
├── faiss_index.bin     (~150 MB)
└── metadata.pkl        (~50 MB)
```

**Время:** 30-60 минут на CPU (один раз!)

### Шаг 2: Поиск (step2_search.py)

```
Запрос пользователя: "уютное кафе с кофе"
    ↓
[Embedding] → вектор запроса
    ↓
[FAISS.search] → топ-200 релевантных отзывов
    ↓
[GroupBy place_id] → группировка по заведениям
    ↓
[Aggregation] → weighted/mean/max
    ↓
Топ-10 заведений
```

**Время:** 50-200 мс

## Примеры запросов

```python
# Атмосфера
search.search_places("уютное кафе с вкусным кофе")
search.search_places("романтическое место для свидания")
search.search_places("тихое место для работы")

# Цена/Качество
search.search_places("недорогой ресторан с большими порциями")
search.search_places("где недорого поесть")

# Особенности
search.search_places("заведение с живой музыкой")
search.search_places("семейное кафе с детской зоной")
search.search_places("модный бар с коктейлями")

# Время
search.search_places("где поесть поздно ночью")
search.search_places("завтрак с утра")

# На казахском
search.search_places("дәмді кофе бар жайлы орын")
```

## Настройка параметров

### config.py

```python
# Модель (можно изменить)
MODEL_NAME = "sentence-transformers/LaBSE"  # Мультиязычная
# MODEL_NAME = "DeepPavlov/rubert-base-cased-sentence"  # Только русский, быстрее

# Параметры поиска
DEFAULT_TOP_K = 10              # Количество результатов
DEFAULT_MIN_REVIEWS = 3         # Минимум релевантных отзывов
DEFAULT_AGGREGATION = "weighted" # weighted/mean/max

# Размер батча для создания эмбеддингов
BATCH_SIZE = 32
```

### При вызове search_places()

```python
results = search.search_places(
    query="уютное кафе",
    top_k=10,              # Сколько заведений вернуть
    min_reviews=3,         # Минимум релевантных отзывов
    aggregation='weighted' # Метод ранжирования
)
```

## Методы агрегации

### weighted (рекомендуется)
```python
final_score = avg_similarity * log(review_count + 1)
```
Баланс между качеством и количеством отзывов.

### mean
```python
final_score = mean(similarity_scores)
```
Только качество, игнорирует количество.

### max
```python
final_score = max(similarity_scores)
```
Находит хотя бы один идеальный отзыв.

## Структура результатов

```python
results = search.search_places(query)
# Возвращает pandas DataFrame:

results.columns:
['name', 'address', 'category', 'rating',
 'final_score', 'avg_score', 'review_count', 'place_id']
```

## Производительность

### Память (во время работы)
- Модель LaBSE: ~500 MB
- Embeddings: ~200 MB
- FAISS индекс: ~150 MB
- DataFrames: ~50 MB
- **Итого:** ~1 GB RAM

### Скорость
- Инициализация: 3-5 секунд
- Поиск: 50-200 мс
- Создание эмбеддингов (первый раз): 30-60 минут на CPU

### Диск
- Исходные данные: ~50 MB
- Кэш: ~400 MB
- Модель: ~500 MB
- **Итого:** ~1 GB

## Частые вопросы

### Q: Нужно ли запускать step1_prepare.py каждый раз?
**A:** Нет! Только один раз. При следующих запусках нажмите `y` для загрузки из кэша.

### Q: Можно ли использовать на обычном ноутбуке?
**A:** Да! 8GB RAM достаточно. Первое создание эмбеддингов займет 30-60 минут, но это делается один раз.

### Q: Как ускорить создание эмбеддингов?
**A:**
1. Используйте GPU версию: `pip install faiss-gpu`
2. Выберите более легкую модель в `config.py`

### Q: Нет результатов по запросу?
**A:**
```python
# Уменьшите min_reviews
results = search.search_places(query, min_reviews=1)

# Или измените aggregation
results = search.search_places(query, aggregation='max')
```

### Q: Веб-интерфейс обязателен?
**A:** Нет! Достаточно терминала или Jupyter ноутбука. Веб-интерфейс (app.py) опционален.

### Q: Как экспортировать результаты?
**A:**
```python
results = search.search_places(query)
results.to_csv('results.csv', index=False, encoding='utf-8')
```

## Что дальше?

### Улучшение качества
1. Экспериментируйте с моделями в `config.py`
2. Попробуйте разные методы агрегации
3. Настройте `min_reviews` под ваши данные

### Расширение функциональности
1. Добавьте фильтры (категория, район, рейтинг)
2. Реализуйте гибридный поиск (semantic + BM25)
3. Добавьте переранжирование (cross-encoder)
4. Реализуйте query expansion

### Масштабирование
1. Quantization для уменьшения размера
2. IVF индекс для больших данных (>1M)
3. Развертывание в облаке (когда понадобится)

## Поддержка

Для вопросов и улучшений обращайтесь к документации:
- [README.md](README.md) - полная документация
- [QUICK_START.md](QUICK_START.md) - быстрый старт
- [search_demo.ipynb](search_demo.ipynb) - интерактивные примеры

---

**Статус:** ✅ Готово к использованию
**Версия:** MVP 1.0
**Дата:** 2024

Приятного использования! 🚀
